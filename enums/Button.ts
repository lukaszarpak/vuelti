export enum ButtonVariant {
  PRIMARY = "primary",
  BLUE = "blue",
  TRANSPARENT = "transparent",
  TEXT = "text",
  WHITE = "white",
  DARK_GRAY = "dark-gray",
  LINK = "link",
}

export enum ButtonType {
  SUBMIT = "submit",
  RESET = "reset",
  BUTTON = "button",
}

export enum ButtonIconPlacement {
  BEFORE = "before",
  AFTER = "after",
}

export enum ButtonHrefTarget {
  SELF = "_self",
  BLANK = "_blank",
}
